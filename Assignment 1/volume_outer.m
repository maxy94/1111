%Assignment 1-Question 2 part 2
%By identifying separate tasks and implementing each of these tasks in separate
%functions.

%This is the task number 1
%Finding the Outer volume

function volumeOuter= volume_outer(outer_radius,length) %outer_radius=r2 , length=L
volumeOuter=pi*length*(outer_radius^2);

end
