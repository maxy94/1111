%Assignment 1-Question 2 part 2
%By identifying separate tasks and implementing each of these tasks in separate
%functions.

%This is the task number 2
%Finding the inner volume

function volumeInner= volume_inner(inner_radius,length) %inner_radius=r1 , length=L
volumeInner=pi*length*(inner_radius^2);

end
